import {createTheme} from '@mui/material/styles';

const theme = createTheme({
     type: 'light',
     primary: {
      main: '#141414',
      contrastText: '#ffffff',
      light: 'rgba(109,109,100,0.7)',
    },
    secondary: {
      main: '#e2d7da',
    },
})



export default theme;